/// <reference types='cypress' />
describe('Registration form:', () => {
  const user = userData
  before(() => {
    cy.visit('automation-practice-form');
  });

  it('fill all field', () => {

  });
});
